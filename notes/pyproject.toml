[project]
name = "seafolk-notes"
version = "0"
description = "Validation plots for the Seafolk sessions. Not a package."
requires-python = ">=3.12"
dependencies = ["matplotlib>=3.9"]

[tool.uv]
package = false
